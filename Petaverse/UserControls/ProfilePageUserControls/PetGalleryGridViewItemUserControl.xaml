<UserControl
    x:Class="Petaverse.UserControls.ProfilePageUserControls.PetGalleryGridViewItemUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Petaverse.UserControls.ProfilePageUserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <MenuFlyout x:Key="PetGalleryGridViewItemUserControlFlyout">
            <MenuFlyout.Items>
                <MenuFlyoutItem Text="Delete Image" 
                                Icon="Delete" 
                                Background="Red"/>
            </MenuFlyout.Items>
        </MenuFlyout>
    </UserControl.Resources>

    <Grid VerticalAlignment="Stretch"
          HorizontalAlignment="Stretch"
          ContextFlyout="{StaticResource PetGalleryGridViewItemUserControlFlyout}">
        <Image  x:Name="PetMedia"
                MinHeight="300"
                Stretch="UniformToFill"
                VerticalAlignment="Stretch"
                HorizontalAlignment="Stretch"
                Source="{x:Bind PetaverseMedia.MediaUrl, Mode=OneWay}"/>
    </Grid>
</UserControl>
